// NOTE(Oliver): stuff!

enum Navigation_Mode {
    NV_COMMAND,
    NV_MAKE,
    NV_EDIT,
    NV_DELETE,
};
struct {
    
    Navigation_Mode mode;
} navigator;

internal void
navigate_graph(){
    
}